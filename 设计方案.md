# 分析仪器智能语音交互系统 设计方案

> 项目：基于 RDK X5 的离子色谱分析仪离线语音交互系统
> 版本：v1.0（2026-09-02）
> 约定：所有开发任务按"工作包"（W01~W24）切分，每个工作包 = 1~1.5 小时的单人工作量，含明确的产出物与验收测试。做完一个勾一个。

---

## 目录

1. [项目目标与范围](#1-项目目标与范围)
2. [总体设计方案](#2-总体设计方案)
3. [硬件方案](#3-硬件方案)
4. [软件方案](#4-软件方案)
5. [分步骤执行计划（工作包）](#5-分步骤执行计划工作包)
6. [测试方案](#6-测试方案)
7. [数据采集设计（为后期微调铺路）](#7-数据采集设计为后期微调铺路)
8. [风险与对策](#8-风险与对策)
9. [后期演进（MVP 之后）](#9-后期演进mvp-之后)
10. [附录](#10-附录)

---

## 1. 项目目标与范围

### 1.1 目标

在 RDK X5 开发板上构建一套**完全离线**的语音交互系统，操作者用普通话自然语言即可完成离子色谱分析仪的常见操作：

- 语音下达仪器指令（设参数、启动/停止、查询状态）→ 转换为仪器操作序列执行
- 语音提问操作问题（"怎么排气泡？"）→ 文字帮助 + 语音播报
- 不依赖网络，端到端响应快（命令类 ≤ 1s）

### 1.2 范围界定

| 项 | 本期（MVP） | 后期演进 |
|---|---|---|
| 语言 | 仅普通话 | 方言适配（M9） |
| ASR | Vosk（需求文档指定第一步） | FunASR Paraformer 微调替换（M7） |
| NLU | 规则优先 + 本地 LLM 兜底 | LLM 场景微调（M8） |
| 仪器 | TCP 协议模拟器调试 → 真机对接 | — |
| 拾音 | 近讲（站仪器前对麦克风说） | — |
| 唤醒词 | 有（可选开启） | — |

### 1.3 MVP 验收标准

1. 域内命令**端到端意图+槽位准确率 ≥ 98%**（词表约束口径，见 §6.1）
2. 端到端时延：命令类 ≤ 1s，问答类 ≤ 3s
3. 覆盖命令域全部意图（附录 A 词表），离子色谱模拟器全流程演示
4. 断网环境全程可用；连续运行 2 小时无崩溃、无内存泄漏

---

## 2. 总体设计方案

### 2.1 系统架构

```
                        RDK X5（8×A55 1.5GHz + 10TOPS BPU，Ubuntu 22.04）
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  USB麦克风 ──► audio采集 ──► VAD断句 ──► ASR流式识别(Vosk,词表约束)      │
│                    ▲                        │                           │
│                    │                   文本(可能含错字)                  │
│              唤醒词KWS                        ▼                          │
│              (可选)              ┌────────────────────────┐             │
│                                  │  NLU 语义理解           │             │
│                                  │  L1 规则引擎(90%命令)    │             │
│                                  │     ↓ 置信不足           │             │
│                                  │  L2 本地LLM兜底(问句)    │             │
│                                  └───────────┬────────────┘             │
│                                              │ 结构化动作JSON             │
│                                              ▼                          │
│                                   ┌─────────────────────┐              │
│                                   │  policy 决策层        │              │
│                                   │  技能映射表 + 安全检查  │              │
│                                   └──┬────────┬─────────┬┘              │
│                                      ▼        ▼         ▼               │
│                                 TTS语音播报  文字帮助   动作序列           │
│                                 (USB音箱)  (串口/SSH屏)  │                │
│                                                          ▼               │
│  ┌──────────────────────────── 执行层 ──────────────────────────────┐    │
│  │  InstrumentClient (TCP socket, 重连/心跳/超时)                    │    │
│  │      └── ProtocolAdapter ←── 按离子色谱参数文档实现编解码          │    │
│  │              ├── SimIC  开发期模拟器（同协议）                     │    │
│  │              └── RealIC 真机离子色谱                              │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  recorder 数据采集模块：录音落盘 + 转写 + 标注侧车文件（贯穿全链路）          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 设计原则

1. **封闭域思维**：仪器操作词汇天然有限（附录 A），ASR 用词表约束、NLU 用意图表、评测用域内口径——98% 指标只在封闭域内现实。
2. **分层解耦**：各模块间只传标准数据结构（音频帧 / 文本 / 动作 JSON），任何一个模块可独立替换（Vosk→Paraformer、模拟器→真机均零上层改动）。
3. **技能与协议分离**：NLU/决策层只面向"仪器技能"（如 `set_flow_rate(1.0)`），协议编解码全部在 `ProtocolAdapter` 内完成。
4. **数据优先**：从第一天起录音落盘、全链路日志，MVP 积累的语料就是 M7/M8 微调的原料。

### 2.3 核心数据流与接口

模块间统一传 JSON / dict，关键接口定义：

```python
# ASR 输出
{"text": "把柱温设到60度", "grammar_hit": True, "confidence": 0.87}

# NLU 输出（统一动作 schema）
{
  "intent": "set_column_temp",      # 意图，见附录A
  "slots": {"value": 60, "unit": "celsius"},
  "source": "rule" | "llm",          # 谁理解的
  "confidence": 0.95
}
# 开放问句：{"intent": "ask_help", "slots": {"topic": "排气泡"}}

# 决策层输出
{
  "reply_text": "好的，柱温已设为60摄氏度",      # TTS 播报
  "help_text": "...",                           # 可选，文字帮助
  "actions": [{"skill": "set_column_temp", "params": {"value": 60}}]
}
```

### 2.4 响应时延预算（命令路径）

| 环节 | 预算 | 说明 |
|---|---|---|
| VAD 端点检测（尾点） | 400ms | silero，静音 400ms 判定说完 |
| ASR 最终结果 | 150ms | 流式，说完即出 |
| NLU 规则匹配 | 50ms | 纯内存词表匹配 |
| 决策 + 执行（模拟器） | 100ms | 本地 TCP |
| TTS 首音频输出 | 300ms | VITS 流式合成首块 |
| **合计** | **≈1.0s** | 问答类走 LLM，另加 1~3s |

---

## 3. 硬件方案

### 3.1 物料清单（BOM）

| 项目 | 规格 | 数量 | 备注 |
|---|---|---|---|
| 主控板 | **RDK X5 8GB 版**套件（含散热、12V电源） | 1 | 4GB 版跑 ASR 大模型+LLM 共存会很紧，务必 8GB |
| TF 卡 | 32GB+ A2 级（写入 ≥100MB/s） | 1 | 系统盘 |
| USB 麦克风 | 近讲电容麦（带拾音指示灯更佳） | 1 | 即插即用 UAC 协议，避免板载音频质量问题 |
| USB 音箱 | 小功率 USB 声卡音箱一体 | 1 | TTS 播报输出 |
| 网线 | 千兆 | 1~2 | X5 ↔ 开发PC/交换机/仪器 |
| 开发 PC | Windows/Linux 均可，需 SSH + 网口 | 1 | 已有；另需 GPU 机器用于后期微调（云 GPU 亦可） |
| HDMI 显示器 | 1080p | 1（可选） | 调试期看界面，后期可无头运行 |
| USB 转 TTL | — | 1（可选） | 串口控制台排障 |

### 3.2 连接拓扑

```
开发PC ──── 网线 ──── RDK X5 ──── 网线 ──── 离子色谱分析仪(真机)
  │                    │  ▲
  │ SSH/SCP调试        │  └─ USB: 麦克风 + 音箱
  └─ 模拟器SimIC(开发期也可跑在PC或X5上)
```

- 开发期：模拟器跑在 X5 本机（localhost:9000）或 PC 上，**协议与真机一致**
- 真机对接期：X5 与仪器接入同一局域网，仪器 IP/端口按参数文档配置
- 板子烧录后设静态 IP，日常 SSH 无头开发

### 3.3 硬件要点

- RDK X5：8× Cortex-A55 @1.5GHz，10 TOPS BPU（BPU 用于 LLM 推理），Ubuntu 22.04 aarch64
- 音频全部走 USB（UAC），插上后 `aplay -l` / `arecord -l` 确认设备号，配置文件里写死设备索引
- 真机仪器的网络参数（IP、端口、协议格式）以用户提供的《参数设置文档》为准，M5 时填入 `config/instrument.yaml`

---

## 4. 软件方案

### 4.1 软件栈选型

| 模块 | 选型 | 版本/模型 | 理由 |
|---|---|---|---|
| 系统 | Ubuntu 22.04 (aarch64) | X5 官方镜像 | 需求指定 |
| 语言 | Python 3.10 | 系统自带 | 生态最全 |
| 音频 IO | sounddevice + numpy | — | PortAudio 封装，跨设备 |
| VAD | sherpa-onnx 内置 silero-vad | — | 快、稳、省 CPU |
| ASR（MVP） | **Vosk** | vosk-model-small-cn-0.22（42M，先跑通）→ vosk-model-cn-0.22（1.3G，冲准确率） | 需求第一步；流式；`SetGrammar` 词表约束是达成 98% 的关键 |
| ASR（微调期） | sherpa-onnx + Paraformer | sherpa-onnx-paraformer-zh-2023-09-14 | FunASR 系，微调管线成熟，支持热词，接口与 Vosk 接近（M7 替换） |
| 唤醒词 | sherpa-onnx KWS | zipformer 关键词模型，自定义拼音关键词 | 离线、低误唤醒 |
| NLU-规则 | 自研规则引擎 | 同义词表 + 模板槽位 + 拼音纠错 | 响应 <50ms、零幻觉、行为可控 |
| NLU-LLM | Qwen2.5-1.5B/3B-Instruct（int4） | BPU 部署（hobot 官方/社区方案）；备选 llama.cpp CPU（慢） | 只兜底开放问句，容忍 1~3s |
| TTS | sherpa-onnx VITS | melo-tts-zh_en（离线中文女声） | aarch64 可跑，首响快 |
| 仪器协议 | 自研 InstrumentClient + ProtocolAdapter | asyncio TCP | 参数文档驱动，模拟器同协议 |
| 训练平台（后期） | FunASR / LLaMA-Factory | PC/云 GPU | M7/M8 用 |

### 4.2 项目目录结构

```
icvoice/                          # 项目根目录（git 管理）
├── config/
│   ├── system.yaml               # 音频设备号、采样率、模型路径、日志级别
│   ├── instrument.yaml           # 仪器 IP/端口/协议参数（真机/模拟器切换）
│   ├── vocabulary.txt            # ASR 词表约束（全部域内说法，一行一条）
│   ├── intents.yaml              # 意图表：意图→模板/同义词/槽位定义
│   └── pinyin_fix.yaml           # 拼音相似纠错映射（电导测→电导池 等）
├── models/                       # 模型文件（gitignore，脚本下载）
│   ├── vosk-small/  vosk-big/  vad/  kws/  tts/
├── src/icvoice/
│   ├── audio/     capture.py  vad.py  wakeup.py        # 采集/断句/唤醒
│   ├── asr/       base.py  vosk_engine.py  paraformer_engine.py
│   ├── nlu/       rule_engine.py  llm_fallback.py  pinyin.py  router.py
│   ├── policy/    policy.py  skills.py                 # 决策+技能映射
│   ├── tts/       tts_engine.py  speaker.py            # 合成+播放队列
│   ├── instrument/ client.py  adapter.py  sim_ic.py    # 执行层
│   ├── data/      recorder.py  logger.py               # 录音落盘+会话日志
│   └── pipeline.py                                      # 主状态机(串起全部)
├── tools/
│   ├── rec_dataset.py            # 批量录音采集工具（建测试集用）
│   ├── label_check.py            # 转写核对工具
│   └── eval_asr.py  eval_e2e.py  # 评测脚本
├── tests/                        # pytest 单测
├── scripts/                      # X5 上的启动/自检脚本
└── docs/                         # 本文档、参数文档笔记、会议记录
```

### 4.3 主状态机（pipeline.py）

```
IDLE ──唤醒词/按键──► LISTENING ──VAD出句──► RECOGNIZING ──文本──► UNDERSTANDING
  ▲                                                                    │
  │                     ┌──────────────────────────────────────────────┤
  │                     ▼                                              ▼
  └──────────────── SPEAKING（TTS播报）◄── 执行/查帮助 ◄────────── DECIDING
                          │ 完毕（可打断）
                          ▼
                        IDLE
```

- LISTENING 超时 8s 无语音 → 回 IDLE；打断（说话即停播）作为 M4 增强，非必需
- 所有环节异常 → 语音提示"没听清/出错了" → 回 IDLE，绝不静默卡死

### 4.4 关键技术点

1. **ASR 词表约束**（98% 的第一支柱）
   `KaldiRecognizer(model, 16000, json.dumps(vocabulary_list))` 把解码空间限制到域内说法；配合 `SetWords(True)` 拿词级置信度。开放文本（问句）切换无语法模式识别。

2. **两级识别路由**（98% 的第二支柱）
   先用词表模式识别；若输出命中词表 → 命令路径；未命中 → 用开放模式重识别一次 → 判定为问句 → 走 LLM。

3. **拼音纠错**（98% 的第三支柱）
   规则引擎先查意图表直接匹配；失败则把文本转拼音，与意图模板算编辑距离，距离 ≤ 阈值即纠错采纳（"电导测"→"电导池"、"淋洗液"同音容错）。

4. **LLM 输出强约束**
   Prompt 模板 + few-shot，要求 LLM 只输出 JSON（意图+槽位），解析失败重试 1 次，再失败降级为"请换种说法"。LLM 永远不直接产生仪器命令，只产出标准动作 JSON，由决策层安全检查（附录 B 参数合法域）后执行。

5. **执行层可靠性**
   TCP 客户端带：连接状态机、断线自动重连（指数退避）、请求超时 2s、命令幂等检查；关键动作（停止采集、启停泵）执行前 TTS 复述确认。

---

## 5. 分步骤执行计划（工作包）

> 每个工作包 1~1.5h，含任务清单、产出物、验收测试。里程碑 M0~M6 = MVP，M7~M9 见 §9。

### M0 环境搭建

**W01 系统烧录与远程开发环境**
- 任务：按官方文档给 X5 烧录 Ubuntu 22.04；配置静态 IP；PC 端 SSH 免密登录；换源装基础包（`git python3-pip build-essential alsa-utils`）
- 产出：X5 可 SSH；`uname -a` 确认 aarch64
- 验收：PC 上 `ssh rdk@<ip> "uname -m"` 输出 aarch64

**W02 音频链路验证**
- 任务：插 USB 麦克风+音箱；`arecord -l`/`aplay -l` 找设备号；`arecord -D hw:X,0 -f S16_LE -r 16000 test.wav` 录 5s，`aplay` 回放；装 sounddevice 写 `scripts/audio_check.py`（录音→回放循环）
- 产出：`config/system.yaml` 中设备号确定；audio_check.py 可用
- 验收：对着麦克风说话回放清晰，无爆音/底噪异常

**W03 Python 项目骨架**
- 任务：建 §4.2 目录结构；git init；写 `requirements.txt`（numpy sounddevice pyyaml pytest requests）；搭 `pipeline.py` 空状态机骨架（各模块先 stub，日志打通）；装 rsync/VSCode-Remote 调试工作流
- 产出：骨架代码跑通 stub 链路（假数据从 IDLE 走到 SPEAKING）
- 验收：`pytest` 空测试通过；stub 流程日志完整

### M1 Vosk 语音识别测试（需求第一步）

**W04 Vosk 小模型跑通**
- 任务：PC 下载 vosk-model-small-cn-0.22 传到 X5；`pip install vosk`（确认有 aarch64 wheel）；写 `src/icvoice/asr/vosk_engine.py`（支持：识别 wav 文件 / 接收音频流）
- 产出：asr_engine 可用，`python -m icvoice.asr.vosk_engine test.wav` 输出转写
- 验收：3 个预录句子（"开始测量""停止采集""查看压力"）转写正确

**W05 实时麦克风流式识别 + VAD**
- 任务：写 `audio/capture.py`（sounddevice 16k mono 帧流）+ `audio/vad.py`（silero-vad，静音 400ms 出句）；接通 麦克风→VAD→Vosk 实时链路；终端打印每句转写
- 产出：实时说、实时出字
- 验收：连续说 5 句命令，全部即时正确输出；测量"说完→出字"时延并记录

**W06 词表约束对比实验**
- 任务：起草附录 A 词表进 `config/vocabulary.txt`；`KaldiRecognizer.SetGrammar()` 开关两模式；写 `tools/eval_asr.py`（输入 wav 目录+标注文件，输出 CER 与句级正确率）
- 产出：eval_asr.py；同一小批录音两种模式初步数据
- 验收：看到词表模式句级正确率明显高于开放模式（预期 90%→98%+ 量级）

**W07 测试集构建**
- 任务：写 `tools/rec_dataset.py`（按键录音→存 wav+标注侧车 json）；用附录 A 词表每人每句 2 遍、2~3 人、安静+轻噪声两种环境，录 150~200 条；整理成 `tests/asr_testset/`
- 产出：域内 ASR 测试集 v1（150+ 条，含标注）
- 验收：目录条数齐全，标注抽查 20 条无误

**W08 大模型 + 正式评测（M1 收口）**
- 任务：换 vosk-model-cn-0.22（1.3G）重复 W06 评测；小 vs 大模型对比表；把最终选型+数据写入 `docs/m1_report.md`
- 产出：M1 评测报告（CER / 句级准确率 / 时延 / 内存占用 四指标两模型对比）
- 验收：选定模型词表模式下域内句级准确率 ≥ 97%（不到则分析错误类型，进 W14 纠错兜底）

### M2 实时交互链路

**W09 唤醒词与会话状态机**
- 任务：部署 sherpa-onnx KWS，选/录唤醒词（如"小谱小谱"，按拼音配置）；pipeline.py 实装 IDLE→LISTENING→…状态机；LISTENING 8s 超时回 IDLE
- 产出：完整交互回合闭环（唤醒→说话→出句→回 IDLE）
- 验收：20 次唤醒成功 ≥ 19 次；说无关话不误唤醒（误唤醒 10 分钟 ≤ 1 次）

**W10 数据采集模块**
- 任务：写 `data/recorder.py`（每句音频 16k wav + sidecar json：时间戳/ASR结果/NLU结果/最终意图/会话ID）+ `data/logger.py`（结构化会话日志）；接入 pipeline 各环节
- 产出：全部交互落盘 `data/sessions/`
- 验收：跑 10 轮对话，wav+json+日志一一对应、时间轴对齐

**W11 噪声环境实测与调优**
- 任务：模拟实验室真实条件（仪器运行声/空调/多人说话）复测；调 AGC 增益、VAD 门限、能量下限；把"误触发率、漏句率、识别率"记入表格
- 产出：调参记录 + system.yaml 最终参数
- 验收：典型噪声下域内句识别率较安静环境下降 ≤ 3 个百分点

### M3 NLU 语义理解

**W12 意图表与槽位设计**
- 任务：按附录 A 细化 `config/intents.yaml`（每个意图：模板句、同义词组、槽位类型/取值域/必填性）；定义动作 JSON schema；评审一遍词表覆盖度（每个意图 ≥ 5 种说法）
- 产出：intents.yaml v1 + schema 文档
- 验收：附录 A 全部意图有定义；抽查 3 个意图的 5 种说法能人工对应

**W13 规则引擎实现**
- 任务：写 `nlu/rule_engine.py`（模板匹配→槽位抽取→未命中走拼音纠错 `nlu/pinyin.py`）；读 intents.yaml 驱动，不硬编码
- 产出：rule_engine + pinyin 模块
- 验收：单测 30 条典型说法全对（含 5 条故意错字被拼音纠回）

**W14 规则引擎单测扩充 + 错误分析**
- 任务：用 M1 测试集的 ASR 真实输出（而非理想文本）灌入规则引擎，统计意图理解准确率；对失败案例补同义词/纠错映射；回归单测
- 产出：意图理解准确率数据（目标 ≥ 98%）+ intents.yaml v2
- 验收：`pytest tests/test_nlu.py` 全绿；真实 ASR 输入下意图准确率达标

**W15 本地 LLM 部署**
- 任务：调研 X5 上 Qwen2.5-1.5B BPU 部署路径（官方 hobot/社区 hbm 模型优先；不可行则 llama.cpp int4 CPU 兜底）；先在 PC 用 llama.cpp/ollama 验证 prompt 模板（系统提示+few-shot+只输出JSON）
- 产出：X5 上可调用的 LLM 服务（进程/库二选一）+ prompt 模板 v1
- 验收：X5 上"怎么排气泡""压力偏高怎么办"两个问句返回合法 JSON（help 类意图）

**W16 NLU 路由集成（M3 收口）**
- 任务：写 `nlu/router.py`（词表命中→规则；未命中→开放问句→LLM；LLM 失败→礼貌降级）；集成进 pipeline；写 LLM JSON 解析失败重试与日志
- 产出：完整 NLU 链路；NLU 集成测试脚本
- 验收：命令类全部走规则（日志确认，时延 <100ms）；10 个问句 8 个以上被 LLM 正确归类

### M4 TTS 与输出

**W17 TTS 部署与播报队列**
- 任务：部署 sherpa-onnx VITS melo-tts-zh_en；写 `tts/tts_engine.py`（文本→音频块）+ `speaker.py`（独立播放线程+队列，SPEAKING 状态，播放时可被打断回 LISTENING）
- 产出：任意文本可语音播出，不阻塞主链路
- 验收：播报"柱温已设为60度"清晰自然；播报中说话 3 次内能打断

**W18 决策层与文字输出（M4 收口）**
- 任务：写 `policy/policy.py`（动作 JSON→复述话术/帮助话术/动作序列三分支；参数合法性检查查附录 B 值域）；帮助类同时输出文字（SSH 终端/日志，后续可接屏）；应答话术表配置化
- 产出：policy 模块 + pipeline 全链路（语音进→语音出+文字出）
- 验收：模拟数据端到端演示："查看柱温"→ 播报当前值；"怎么校准"→ 播报+文字帮助

### M5 执行层

**W19 协议分析与模拟器**
- 任务：研读《参数设置文档》，把协议格式（帧头/字段/校验/应答）写成 `docs/protocol_notes.md`；按文档实现 `instrument/sim_ic.py` 模拟器（TCP server，维护仪器状态表：柱温/流速/压力/采集状态…，收到设置命令改状态并应答）——若已有官方协议模拟软件则以其为准，sim_ic 作为回归测试替身
- 产出：sim_ic.py + protocol_notes.md
- 验收：用 Python socket 手工发包，设置柱温 60 → 查询返回 60

**W20 InstrumentClient 协议适配**
- 任务：写 `instrument/client.py`（asyncio TCP：连接状态机、2s 超时、断线重连指数退避、心跳）+ `instrument/adapter.py`（技能→协议帧编解码，读写 instrument.yaml 切换 sim/real）
- 产出：client + adapter，单测覆盖编解码与超时重连
- 验收：`pytest tests/test_instrument.py` 全绿（含断线重连用例）；对 sim_ic 完成设置/查询/启停三类操作

**W21 技能映射**
- 任务：写 `policy/skills.py`（动作 JSON → 一次或多次仪器调用；查询类把仪器返回值格式化为播报文本，如"当前柱温60度，压力8.2兆帕"）；联动 TTS
- 产出：全部附录 A 意图的技能实现
- 验收：语音→执行→播报全链路对 sim_ic 演示 10 条命令，仪器状态表变化全部正确

**W22 端到端集成调试（M5 收口）**
- 任务：pipeline 全链路连调；加执行失败分支（仪器无应答/拒绝执行→TTS 报错）；长时间运行稳定性（挂 1 小时跑脚本）
- 产出：联调问题清单+修复；稳定性记录
- 验收：连续 1 小时 100 轮脚本对话零崩溃；单轮命令端到端 ≤ 1s（实测记录）

### M6 评测与 MVP 交付

**W23 端到端验收测试**
- 任务：建 e2e 测试集（播放测试集 wav 进系统→断言 sim_ic 状态/播报文本）；覆盖全部意图 × 2 种说法 ≈ 80 条；补边界用例（参数越界、无应答、中途打断）
- 产出：eval_e2e.py + e2e 报告
- 验收：端到端意图+槽位准确率 ≥ 98%

**W24 MVP 演示打磨与交付（MVP 收口）**
- 任务：开机自启（systemd）；错误场景话术完善；写用户操作说明 1 页；整体演示彩排（设参数→启动采集→查询→语音报告→帮助问答）
- 产出：可交付 MVP + 演示脚本 + M6 报告（验收标准逐条对照）
- 验收：断网环境完整演示一遍全流程

---

## 6. 测试方案

### 6.1 指标定义（与验收标准对齐）

| 指标 | 定义 | 工具 | 目标 |
|---|---|---|---|
| ASR 句级准确率 | 转写（词表模式下）与标注完全一致的比例 | eval_asr.py | ≥ 98%（域内测试集） |
| ASR CER | 字错率，仅作观察项 | eval_asr.py | 记录，不设硬指标 |
| 意图+槽位准确率 | NLU 输出 JSON 与预期一致 | W14/eval_e2e.py | ≥ 98% |
| **端到端准确率** | 语音进 → 仪器状态/播报正确的比例（最终验收口径） | eval_e2e.py | ≥ 98% |
| 命令时延 | 说完→仪器动作完成 | 日志时间戳 | ≤ 1s |
| 问答时延 | 问完→播报开始 | 日志时间戳 | ≤ 3s |
| 误唤醒率 | 静默/噪声 1 小时内自启次数 | W09 实测 | ≤ 1 次 |
| 稳定性 | 连续运行崩溃次数 | W22/W24 | 2h 零崩溃 |

> 口径说明：98% 指**封闭域内、词表约束下、端到端意图+槽位**口径。开放转写不承诺 98%，此口径已在方案讨论中与需求确认。

### 6.2 测试分层

```
单测(pytest)         模块级：NLU规则(30+条)、协议编解码、拼音纠错、状态机迁移
   ↓
ASR 评测(eval_asr)   数据集级：M1 测试集 150+ 条，两模式两模型对比
   ↓
NLU 评测             ASR 真实输出灌入（W14），模拟"带错字文本"
   ↓
端到端(e2e)          播放 wav → 断言 sim_ic 状态 + 播报文本（W23）
   ↓
人工实测             真人近讲、噪声环境、长时间运行、边界场景
```

### 6.3 回归策略

- intents.yaml / vocabulary.txt / 纠错表每次修改必须跑 `pytest` + eval_asr 全量回归，防止"修一个坏一个"
- e2e 报告留存版本化（`docs/e2e_日期.md`），MVP 前每周跑一次

---

## 7. 数据采集设计（为后期微调铺路）

**从 W10 起自动积累，MVP 期间零额外成本：**

| 数据 | 内容 | 用途 |
|---|---|---|
| 语音-转写对 | 每句 wav + ASR 结果 + 人工纠正文本（抽查标注） | M7 Paraformer 微调 + 热词挖掘 |
| 失败案例库 | 识别错/理解错/执行失败的完整会话 | 微调重点语料、纠错表扩充 |
| QA 语料 | LLM 兜底的问句 + 最终采纳的回答 | M8 LLM LoRA 微调 |
| 领域术语表 | 淋洗液、抑制器、电导检测、柱温箱… | ASR 热词 + LLM prompt 词典 |

- 存储估算：16k mono 16bit ≈ 32KB/s，每天 1 小时语音 ≈ 115MB，X5 TF 卡可滚动保留，定期 `rsync` 回 PC 归档
- 标注规范：`data/sessions/` sidecar json 增加人工字段（`corrected_text`、`final_intent_ok`），W10 预留字段

---

## 8. 风险与对策

| # | 风险 | 概率 | 对策 |
|---|---|---|---|
| 1 | Vosk 词表模式下仍达不到 98% | 中 | 拼音纠错兜底（W13）；极端句式补录训练（M7）；最终指标有端到端口径缓冲 |
| 2 | Qwen 在 X5 BPU 部署不顺 | 中 | 备选路线：llama.cpp int4 CPU（1~3s 可接受）；再不行 LLM 跑在 PC（演示时局域网内，真机部署后议） |
| 3 | 仪器协议文档不完整/真机行为与文档不符 | 中 | W19 先模拟器；真机对接前找仪器厂商确认报文样例；adapter 层隔离变更 |
| 4 | 实验室噪声导致识别率下降 | 中 | W11 调参；近讲+VAD 门限；必要时换指向性麦克风（低成本追加） |
| 5 | TTS 首响慢/音质差 | 低 | 换更小 VITS 模型或预合成常用应答句（缓存命中 0 时延） |
| 6 | X5 音频驱动与 USB 设备兼容性问题 | 低 | W02 第一时间验证，硬件早买早试 |
| 7 | 微调数据量不足 | 中 | W10 全量落盘；rec_dataset.py 可随时补录；M7 前评估量再定训练策略 |

---

## 9. 后期演进（MVP 之后）

### M7 ASR 场景微调（约 2~3 周）

1. 迁移运行时：Vosk → sherpa-onnx + Paraformer（`asr/paraformer_engine.py`，接口对齐 `base.py`，上层零改动）
2. 数据：MVP 期落盘语料清洗 → 训练/验证集划分（≥ 2000 条域内语音为佳）
3. 训练（PC/云 GPU）：FunASR Paraformer 微调 + **领域热词**（离子色谱术语表）+ 错误案例加权
4. 导出 onnx → sherpa-onnx 跑 X5 → 用 M1 测试集 + 新采集集回归对比 Vosk，**择优替换**
5. 产出：微调后 ASR，域内准确率目标 99%+，开放转写明显改善

### M8 LLM 场景微调（约 2 周）

1. 语料：MVP 期 QA 记录 + 人工扩写领域问答对（数百条起）
2. LLaMA-Factory LoRA 微调 Qwen2.5-1.5B（一张消费级 GPU 即可）
3. 量化 → 重新部署 X5 → 帮助问答准确率/格式合规率评测
4. 产出：领域化 LLM，回答更准、话术风格统一

### M9 方言适配（范围另定）

1. 复用 M7 管线：采集目标方言域内语音（同 rec_dataset.py 流程）→ 混合普通话数据微调
2. 评估 FunASR 方言模型（如 SenseVoice 支持粤语）直接迁移的成本
3. 范围明确前不做承诺；管线已就绪，主要工作量在数据采集

---

## 10. 附录

### 附录 A 命令域词表（初稿，W12 定稿）

| 类别 | 意图 | 示例说法（每种意图至少 5 种） |
|---|---|---|
| 方法 | switch_method / query_method | "切换到方法一""调用标样序列""现在用的什么方法" |
| 泵 | set_flow_rate / pump_on / pump_off | "流速设到1毫升每分钟""把泵打开""关泵" |
| 温控 | set_column_temp / query_temp | "柱温设到60度""把温度调到三十""现在柱温多少" |
| 检测 | set_range / set_suppressor_current / zero_baseline | "量程调到一""抑制器电流设到50毫安""基线调零" |
| 进样 | inject_valve / start_autosampler | "进样阀切到载流""开始自动进样" |
| 采集 | start_run / stop_run | "开始测量""开始采集""停止""别测了" |
| 状态 | query_status / query_pressure | "现在什么状态""压力多少""平衡好了吗" |
| 帮助 | ask_help | "怎么排气泡""压力偏高怎么办""校准步骤是什么" |
| 系统 | repeat / cancel / confirm | "再说一遍""算了""确认" |

### 附录 B 参数安全值域（W12/真机文档定稿）

| 参数 | 允许范围 | 越界处理 |
|---|---|---|
| 流速 | 0.1 ~ 5.0 mL/min | 拒绝 + 播报合法范围 |
| 柱温 | 10 ~ 80 ℃ | 同上 |
| 抑制器电流 | 10 ~ 300 mA | 同上 |
| （其余参数 W19 读参数文档后补全） | | |

### 附录 C 参考资料与工具

- RDK X5 资料：https://developer.d-robotics.cc/information ｜ 社区论坛：https://developer.d-robotics.cc/forum ｜ 亚博智能教程：https://www.yahboom.com/study/RDK_X5 （提取码 rdk5）
- Vosk：https://alphacephei.com/vosk （模型下载、Python API、SetGrammar）
- sherpa-onnx：https://github.com/k2-fsa/sherpa-onnx （VAD/KWS/VITS/Paraformer 全家桶，有 aarch64 预编译包）
- FunASR：https://github.com/modelscope/FunASR （Paraformer 微调、热词）
- LLaMA-Factory：https://github.com/hiyouga/LLaMA-Factory （M8 LoRA 微调）

### 附录 D 当前进度看板

- [ ] M0 环境搭建（W01~W03）
- [ ] M1 Vosk 测试（W04~W08）← 需求文档第一步
- [ ] M2 实时链路（W09~W11）
- [ ] M3 NLU（W12~W16）
- [ ] M4 TTS（W17~W18）
- [ ] M5 执行层（W19~W22）
- [ ] M6 MVP 交付（W23~W24）
- [ ] M7 ASR 微调 / M8 LLM 微调 / M9 方言
